var i;

var list = [];
var n = 5;
each (i = 0; i < n; i++) {
    var input = integer(get("Enter value: "));
    list += [input];
}

show("ascending: " + sorted(list));
show("\n");
show("descending: " + sorted(list, NO));